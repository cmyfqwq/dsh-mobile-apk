# 0.13.3 第一轮回归记录（2026-09-06，16416 竖屏；16384 横屏待复测）

## 已验证 PASS

| 项 | 证据 |
|---|---|
| 快照构建 + 门禁 | Fast 链全绿：统一补丁 12/12、overlay 抽验 269 断言+presets+pi-drift marker、注入 10 包挂载集、third-party/elf/secrets、gradle |
| 引擎 overlay 落位 | 真实快照 check-engine-overlay PASSED（220 packages + 17 vendorTop + 24 nested + pi-ai pin 0.85.1 + keepUnpublished 4） |
| pi-catalog-diff | 0.84.2→0.85.1：删 125/增 212（报告 .deploy-tmp/pi-catalog-diff-report.md） |
| 鉴权 P0 token 交换 | engine.log token 行解析 → GET /?token= → 303 Set-Cookie（logcat: cookie acquired via token exchange）；宿主侧独立复现 303+同名 cookie |
| 探活兼容 | GET / 401 视作 alive；/api/android/privilege/status exact 路由 200（零鉴权面存活） |
| 新 RPC wire | POST /api/session/create|list|prompt（斜杠端点 + args 包裹）全 200/ok |
| AI 实测 read（用户硬要求） | @"/storage/emulated/0/Download/dsh-0133-read-test.txt"（工作目录外）→ 模型调 read → 内容逐字读回（isError:false）——权限链 30+ fullAccess 实证 |
| 重启 cookie 复用 | force-stop + 重启后无新交换日志、页面直接挂载（cookie 跨引擎重启有效） |
| model-sync 在场 | 设备 files/home/.dsh/models-store.json 已生成（插件已在跑） |
| 闸门管控 | 第 3/4/5 次解压均按指纹翻转+备份消失管控（~9-12 分钟），零抢跑 |

## 修复闭环

1. **坑 38（rc.2 锁定运行时补丁抹新引擎代码）**：SPJ/ATT 重出（rc.1 源+rename 回退 delta）、fs-local/primitives 退役——prompt 链复活前置。
2. **坑 39（store-rehome boot 硬阻断）**：ui-responsive 0.1.13 迁移到 client-store 内联 + RUNTIME_STORE_EXEMPTION 退役 + slots-augment.d.ts 自备类型增强——"Failed to load plugins" 消失（截图实证前后对比）。
3. **坑 41（overlay 登记表漏项）**：primitives 补登记（audit-manifest 对账法固化）。

## 遗留问题（本轮未闭环，下一轮继续）

- **坑 43（0.13.3 新增）：rc.1 boot 管线白屏静默挂起**——新 boot 会话（rev 随机 nonce 每启动更换）下：READY resolved、entries 50 全部 combo 200、ModuleLoader mode=live、__DSH_TRANSPORT__ undefined（设计内可选 seam）——但 root 挂载永不发生、零 console 输出、create 循环内某处静默等待。已排除：旧会话 URL（reload 换新 rev 无效）、API 兼容（dist 全 WebView110 兼容 API）、combo serve（50/50 200）、cookie 面（P0 交换正常）。下一轮手段：① Page.addScriptToEvaluateOnNewDocument hook cordis 内部 await 点逐插件二分；② 对照官方桌面宿主（Windows exe）同 bundle 的 boot 时序；③ 检查 graph 拓扑死锁（inject 等待环）——上轮 model-sync/undo/marketplace 为 0.13.0~rc.2 时代插件，需逐一在 rc.1 下验证 apply 依赖面。
- 16384 横屏装机复测（等竖屏闭环后一轮过）。
- W9 剩余：市场徽章/过滤器、undo、设置页全屏、回滚演练、-ExportSnapshots 发布构建。

## 环境备注

- 16416 已授权 MANAGE_EXTERNAL_STORAGE（appops allow，AI read 测试前置）
- 测试文件：/storage/emulated/0/Download/dsh-0133-read-test.txt
- 会话：session-2658535f（AI read 实测会话，session.jsonl.zstd 有 read 工具调用铁证）
