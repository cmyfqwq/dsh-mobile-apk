# 0.13.7 全量认证记录（2026-09-10）

> 认证人：AI 开发助手。范围：x86_64 模拟器（MuMu / Android 15 / WebView 110）+ 全门禁 + 双 ABI 纯净构建 + 发布资产一致性。
> arm64 真机验证按用户要求由用户执行；本文件是发版判定的证据台账。

## 1. 结论

| 认证项 | 结果 | 证据 |
|---|---|---|
| PR 合并 | 通过 | 子仓 #14/#20/#5、apk #146/#147、协调仓 #34/#35，CI 绿后合并、分支已删 |
| 统一补丁（vendor） | 通过 | 双 ABI 均 `apply-patches: ALL OK（13/13，mode=check）`（新增 undo-E8；attachment-formats 已退役） |
| 引擎树补丁 + 行为回归 | 通过 | `引擎树补丁就位（5 项 marker 在场）` + `boot-pending-G1: pass=5 fail=0` / `pi-toolcall-G2: pass=8 fail=0`（F3/F4 新增，F1 退役） |
| 引擎 overlay 抽验 | 通过 | `ENGINE-OVERLAY CHECK PASSED（319 包版本断言 + presets 在场；引擎 0.1.5-rc.1）` |
| 单 pass 注入 + 挂载集 | 通过 | `patch mounts ok`（11 个注入包全部挂载） |
| 权限模式门禁 | 通过 | arm64 `52581` 文件 / x86_64 同源（`SNAPSHOT_MODE_CHECK_PASSED`） |
| 第三方 / elf / 机密 | 通过 | 门禁链全绿（未拒打包） |
| gradle 双 ABI | 通过 | arm64 161.2MB / x86_64 158.41MB `BUILD SUCCESSFUL` |
| 快照资产一致性 | 通过 | `PASS arm64 27314fa0…` / `PASS x86_64 5e40948f…`（与 APK 内嵌同源） |
| 发布资产 | 通过 | Release `v0.13.7`（非 draft / 非 prerelease）15 资产：双 APK + 双快照 xz/sha256 + 8 插件 tgz + MANIFEST.txt；发布说明只做 release body |
| 版本口径统一 | 通过 | 见 §3 |
| 设备回归（release x86_64 包） | 通过 | 见 §2 |
| arm64 真机 | 待用户执行 | 设备未接入 |

## 2. 设备回归（release x86_64 包，指纹 `5e40948f…`）

- 装机后快照事务化刷新：指纹 `f754ef60…` → `5e40948f…`，与 Release `snapshot-x86_64.tar.xz` 同源。
- `scripts/verify-webview-015.mjs`：**26/26 ALL PASS**（移动形态标记、框架根、右栏列、顶栏开关、离屏抽屉、拖拽手柄隐藏、会话头部 corner、右栏键、我们的打开入口、桥退役、菜单注入项、附件按钮未遮蔽、名册含 ui-layout + ui-responsive、7 项 polyfill 活性、迭代器助手 map→toArray、全部内联脚本可解析、documentpreview 条目在场）。
- `verify-engine-tree.mjs`：**13/13 PASS**（F3/F4/F2/G1/G2、SPJ/ATT 运行时补丁、ui-responsive 0.2.0、host-web-compat 0.1.12 与其装载期断言、引擎进程唯一）。
- 打开方式桥：工作区文件/目录与 `/sdcard/Download/*.txt` 均 `{ok:true}` 并实际拉起系统选择器；应用私有区仍 `not-allowed`。
- 360×780 度量覆盖：无横向溢出；模型选择器由上游自带方案折叠（w 147→46）。
- UI 冗余核对：composer「添加附件」仅 1 个、`.dshaf-btn` 不存在、名册无 `dsh-attachment-formats`；快照徽章 20×20、无可见文本、`title/aria` 带「已存 N 份快照」。

## 3. 版本口径（用户定例：内部界面与实际版本必须一致）

| 显示面 | 取值 | 实测 |
|---|---|---|
| `dumpsys package` | `versionName=0.13.7` / `versionCode=34` | 通过 |
| 桥 `androidBridge.version()` | `BuildConfig.VERSION_NAME` | 返回 `0.13.7` |
| 引导页版本标签 | `"v" + BuildConfig.VERSION_NAME` | 同源 |
| 诊断日志 | `app: … (versionCode …)` | 同源 |
| 引擎侧环境配方 | `process.env.DSH_APP_VERSION`（壳侧注入 `BuildConfig.VERSION_NAME`） | 引擎进程环境实测 `DSH_APP_VERSION=0.13.7` / `DSH_APP_VERSION_CODE=34` |
| APK / Release 命名 | gradle `versionName` | `dsh-mobile-apk-v0.13.7-*.apk` / tag `v0.13.7` |

全仓扫描（壳侧 `app/src/main` 资源与源码、`plugins/`、注入层 `src/`）确认无其他用户可见版本字面量；
`android-linux-env` 配方里原有的硬编码 `'0.13.0'` 已改为读环境变量（读不到回落 `'unknown'`）。

## 4. 待办 / 已知限制

1. arm64 真机验证（用户执行）：安装 + 引擎起 + 附件上传 + `android_web_dump` + 截屏内联。
2. `web-frontend-index.html` 运行时补丁资产当前与引擎模板逐字同源（空转），建议退役（见 RUNTIME-PATCHES.md §2）。
3. 一处被捕获的 `ValidationError: $.preference expected light but got system`（服务端注入的主题偏好脚本与某客户端 schema 口径差异），渲染与主题切换正常，未深挖。
4. 退役 `dsh-attachment-formats` 后 Office 结构化 / 扫描件 OCR 能力下线（上游文档预览覆盖 PDF/文本/图片）。